# Content directory

This directory is intentionally present before migration.

Files whose names begin with `_` are excluded from the Astro content collection loader.
Migrated public content will be added as Markdown/MDX after WXR normalization and validation.
